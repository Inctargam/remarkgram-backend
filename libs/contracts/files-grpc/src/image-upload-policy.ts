export const MIN_IMAGES_PER_UPLOAD_REQUEST = 1;
export const MAX_IMAGES_PER_UPLOAD_REQUEST = 10;

export const MIN_IMAGE_SIZE_BYTES = 1;
export const MAX_IMAGE_SIZE_BYTES = 20 * 1024 * 1024;

export enum ImageContentType {
  JPEG = 'image/jpeg',
  PNG = 'image/png',
}
